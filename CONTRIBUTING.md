<!-- محرّر بشريًا: العربية أساسية، والإنجليزية نسخة مرافِقة (CONTRIBUTING.en.md). الشيفرة والأوامر تبقى إنجليزية. -->

# المساهمة في quran-api-unified

**العربية** · [English](./CONTRIBUTING.en.md)

شكرًا لرغبتك في المساهمة. أكثر المساهمات فائدةً هي **إضافة مُحوّلات لمصادر جديدة**، وهذا
الدليل يجعل ذلك الطريق واضحًا. وكلُّ ما يأتي هنا محكومٌ بسير العمل نفسه الذي يتّبعه
القائمون على المكتبة: [`docs/workflow.md`](./docs/workflow.md).

## الإعداد المحلّي

```bash
pnpm install
pnpm lint && pnpm typecheck && pnpm test && pnpm build && pnpm pkg:check && pnpm docs:build
```

يجب أن تنتهي كلُّ هذه الأوامر بنجاح (خروجٌ بالرمز 0) قبل أن تبدأ.

## سير العمل

كلُّ تغيير — مُحوّلٌ جديد، أو ميزةٌ في النواة، أو إصلاح — يتّبع المراحل الموصوفة في
[`docs/workflow.md`](./docs/workflow.md): موجزُ المواصفات، ثمّ الفرع، ثمّ التنفيذ من
الطبقات الداخلية أوّلًا، ثمّ الاختبارات المرافِقة، ثمّ البوّابات المحلّية، ثمّ Changeset،
ثمّ طلب الدمج، ثمّ الإصدار. افرع من `main` باستخدام النمط `ibrahim/<issue>-<slug>`.

## عقد المُحوّل

المُحوّل **نقيٌّ ووصفيّ**: لا يتّصل بالشبكة بنفسه، ولا يقرأ `process.env`. هو يَصِف النداء
فحسب:

1. معرّفٌ فريد `id` بصيغة snake_case، واسمٌ ظاهر `name`؛
2. قائمة `capabilities` بأنواع المحتوى التي يخدمها؛
3. لكلّ نوعٍ من المحتوى: `buildUrl(query, ctx)` يبني الرابط، و`transform(raw, query, ctx)`
   **دالةٌ نقيّة** تُحوّل الاستجابة الخام إلى البنية الموحّدة، مع `responseType` (افتراضه
   `json`، و`text` للمصادر النصّية الخام) و`useProxy` إن احتاج المصدرُ وسيطًا؛
4. علامة `auth` لبيان حاجته إلى اعتماد (`none` أو `apiKey` أو `oauth2-client`).

```ts
export interface Adapter {
  id: string                    // snake_case, unique, e.g. 'alquran_cloud'
  name: string
  capabilities: Capability[]    // 'text' | 'audio' | 'translation' | 'tafsir'
  auth?: 'none' | 'apiKey' | 'oauth2-client'
  text?: CapabilityHandler<VerseQuery, UnifiedVerse>
  audio?: CapabilityHandler<AudioQuery, UnifiedAudio>
  translation?: CapabilityHandler<TranslationQuery, UnifiedTranslation>
  tafsir?: CapabilityHandler<TafsirQuery, UnifiedTafsir>
}
```

النداءُ الفعليُّ للشبكة يقع في طبقة الجلب وحدها (`core/http.ts`)؛ فالمُحوّل لا يستورد
`core/http.ts` أبدًا. هذه هي الحدود التي يفرضها الـ linter.

## إضافة مزوّد جديد

1. اجلب التوثيق الحاليّ لواجهة المزوّد، ونفّذ نداءً حقيقيًا واحدًا، واحفظ الاستجابة في
   `test/fixtures/<id>/`.
2. اكتب صفحة المزوّد `docs/providers/<id>.md` (ونسختها الإنجليزية `<id>.en.md`) من القالب:
   الرابط الأساس، ونقاط النهاية، والمعاملات، والاعتماد، وحدود المعدّل، وبنية الاستجابة
   الخام، **وسبب** تحويلها على هذا النحو.
3. أضِف `src/adapters/<id-kebab>.ts` منفِّذًا للمنفَذ (port)، وسجّله في
   `src/adapters/index.ts`.
4. الاختبارات: اختبر `transform` على العيّنة المسجّلة (صحّة التحويل، والمدخلات المشوَّهة)،
   وصحّة `buildUrl`؛ وأضِف المزوّد إلى اختبارات المسارات الإلزامية ذات الصلة.
5. أضِف Changeset يصف الإضافة.

## المُحوّلات التي تتطلب اعتمادًا

الأصل أنّ المكتبة تعمل دون مفاتيح. فإن احتاج المزوّد إلى اعتماد، اضبط `auth` على `apiKey`
أو `oauth2-client`، واقرأ بيانات الاعتماد من `ctx.credentials` **فقط** (يمرّرها المستخدم
مفتاحةً بمعرّف المزوّد)، ووثّق المفاتيح المطلوبة في صفحة المزوّد وفي `.env.example` (تُستعمَل
في مجموعة الاختبار الحيّ وحدها). والمُحوّل الذي ينقصه اعتمادُه يُتخطَّى في الاختيار
التلقائيّ، ولا يُطلق خطأً إلا إذا طُلب بالاسم صراحةً.

## اختبار مُحوّلك

- `transform` يُختبر على العيّنات المسجّلة دون أيّ اتصالٍ بالشبكة.
- `buildUrl` يُختبر لصحّته مع المرجع، وبيانات الاعتماد، والوسيط.
- مجموعة الاختبار الافتراضية (وهي ما يشغّله التكامل المستمرّ) تعمل دون شبكة؛ أمّا فحص
  الانحراف الحيّ فاختياريّ عبر `pnpm test:live`.

## متطلبات التوثيق

- **توثيق TSDoc على كلّ رمزٍ عامّ** — منه يُولَّد المرجع البرمجيّ.
- **لكلّ مُحوّلٍ صفحتُه** `docs/providers/<id>.md`؛ فطلب الدمج الذي يضيف مُحوّلًا بلا
  صفحته لن يُدمج.
- إن غيّرت بنيةً أو تسميةً، فحدّث التوثيق المتأثّر في طلب الدمج نفسه.

## الالتزامات وChangesets وطلبات الدمج

- **Conventional Commits**، ونطاقُ الالتزام هو الوحدة الأكثر تأثُّرًا، مثل:
  `feat(adapters): add mp3quran audio adapter` و`fix(core): honor per-concern timeout`.
- كلُّ تغييرٍ يمسّ المستخدم يرافقه **Changeset** (`pnpm changeset`) بمستوى الترقية الصحيح
  (`feat`=MINOR، و`fix`=PATCH، وقبل الإصدار 1.0 يرفع الكسرُ MINOR).
- الدمج **بالضغط (squash)**، وعنوانُ طلب الدمج هو الالتزام، ويجب أن يوافق Conventional
  Commits.
- قواعد Git الصارمة: لا `git add -A` أبدًا، ولا التزامٌ لـ `.env` أو ملفّات البناء، ولا
  `--no-verify`.

## الترخيص

بمساهمتك توافق على أن تكون مساهماتُك مرخّصةً بموجب ترخيص المشروع [MIT](./LICENSE).
